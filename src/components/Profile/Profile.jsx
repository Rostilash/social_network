import React from "react";

export const Profile = ({ props }) => {
  return (
    <div>
      {/* <ProfileInfo /> */}
      {/* <MyPosts posts={props.profilePage.posts} newPostText={props.profilePage} dispatch={props.dispatch} /> */}
    </div>
  );
};
